# Set to your deployed FastAPI backend (HuggingFace Space, Render, Azure).
# When empty, the frontend runs in demo mode using simulated predictions.
VITE_API_URL=https://your-username-eyevision.hf.space